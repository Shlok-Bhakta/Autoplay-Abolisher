const autoplayPreviewsToggle = document.getElementById("id-AutoplayPreviews");
const autoplayEpisodesToggle = document.getElementById("id-AutoplayEpisodes");
const autoplayMoviesToggle = document.getElementById("id-AutoplayRecommendedMovies&Series");

if (autoplayPreviewsToggle) {
    autoplayPreviewsToggle.click();
}
if (autoplayEpisodesToggle) {
    autoplayEpisodesToggle.click();
}
if (autoplayMoviesToggle) {
    autoplayMoviesToggle.click();
}